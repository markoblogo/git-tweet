export default function HomePage() {
  return (
    <section className="card">
      <h1>git-tweet</h1>
      <p>
        Conservative auto-posting for meaningful development milestones only: releases,
        first public release, major semver jumps, and valid semver tags.
      </p>
      <small>
        MVP policy: low-noise, deterministic, no AI generation, no approval flow, auto-post only.
      </small>
    </section>
  );
}
